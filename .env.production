# Production Environment Variables
# Set these in your deployment platform (Vercel/Railway)

# Frontend API URL (will be your production domain)
NEXT_PUBLIC_API_URL=https://your-domain.com

# Admin Credentials (CHANGE BEFORE DEPLOYING!)
ADMIN_EMAIL=admin@yourdomain.com
ADMIN_PASSWORD=your_secure_production_password

# Node Environment
NODE_ENV=production

# Database (Optional - for MongoDB integration)
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/database
